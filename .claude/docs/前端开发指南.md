# 前端开发指南

> **重要提示**: 本项目（RuoYi-Vue-Plus）是**纯后端项目**，不包含前端代码。
> 前端项目（plus-ui）独立维护，本文档说明后端与前端的协作规范。

最后更新: 2026-02-09

---

## 🏗️ 项目架构说明

### 前后端分离架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端项目（独立仓库）                      │
├──────────────────┬──────────────────────────────────────────┤
│     plus-ui      │  Vue 3 + TypeScript + Element Plus       │
│   （PC 后台管理） │  独立仓库，独立部署                        │
└────────┬─────────┴──────────────────────────────────────────┘
         │
         │  HTTP API 调用
         ▼
┌─────────────────────────────────────────────────────────────┐
│                RuoYi-Vue-Plus（本项目）                       │
│                    纯后端 Java 服务                           │
├─────────────────────────────────────────────────────────────┤
│  ruoyi-admin (启动入口)                                       │
│  ruoyi-modules (业务模块: system, demo, workflow, job...)    │
│  ruoyi-common (通用工具: 24 个子模块)                         │
└─────────────────────────────────────────────────────────────┘
```

### 项目职责划分

| 项目 | 技术栈 | 职责 | 仓库 |
|------|--------|------|------|
| **RuoYi-Vue-Plus**（本项目） | Java + Spring Boot 3 | 后端 API、业务逻辑、数据库 | 当前仓库 |
| **plus-ui** | Vue 3 + TypeScript | PC 端后台管理界面 | 独立仓库 |

---

## 📡 后端 API 规范

### API 路径规范（RESTful）

后端提供的 API 遵循 RESTful 规范：

| 操作 | HTTP 方法 | 路径格式 | 示例 |
|------|----------|---------|------|
| 分页查询 | GET | `/模块/实体/list` | `GET /demo/xxx/list` |
| 获取详情 | GET | `/模块/实体/{id}` | `GET /demo/xxx/123` |
| 新增 | POST | `/模块/实体` | `POST /demo/xxx` |
| 修改 | PUT | `/模块/实体` | `PUT /demo/xxx` |
| 删除 | DELETE | `/模块/实体/{ids}` | `DELETE /demo/xxx/1,2,3` |
| 导出 | POST | `/模块/实体/export` | `POST /demo/xxx/export` |

### 响应格式

后端统一返回 `R<T>` 格式：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": { ... }
}
```

### 分页响应格式

分页查询返回 `TableDataInfo<T>` 格式：

```json
{
  "code": 200,
  "msg": "查询成功",
  "rows": [ ... ],
  "total": 100
}
```

---

## 🔐 权限标识规范

后端使用 `@SaCheckPermission` 注解定义权限，前端使用对应的权限标识：

### 权限命名规则

```
模块:实体:操作
```

### 标准权限列表

| 操作 | 权限标识 | 后端注解 | 前端指令 |
|------|---------|---------|---------|
| 列表查询 | `demo:xxx:list` | `@SaCheckPermission("demo:xxx:list")` | `v-hasPermi="['demo:xxx:list']"` |
| 详情查询 | `demo:xxx:query` | `@SaCheckPermission("demo:xxx:query")` | `v-hasPermi="['demo:xxx:query']"` |
| 新增 | `demo:xxx:add` | `@SaCheckPermission("demo:xxx:add")` | `v-hasPermi="['demo:xxx:add']"` |
| 修改 | `demo:xxx:edit` | `@SaCheckPermission("demo:xxx:edit")` | `v-hasPermi="['demo:xxx:edit']"` |
| 删除 | `demo:xxx:remove` | `@SaCheckPermission("demo:xxx:remove")` | `v-hasPermi="['demo:xxx:remove']"` |
| 导出 | `demo:xxx:export` | `@SaCheckPermission("demo:xxx:export")` | `v-hasPermi="['demo:xxx:export']"` |

---

## 📊 数据类型映射

### Java 与 TypeScript 类型对应

| Java 类型 | TypeScript 类型 | 说明 |
|----------|----------------|------|
| `Long` | `number \| string` | 大数字建议用 string 避免精度丢失 |
| `Integer` | `number` | - |
| `String` | `string` | - |
| `Boolean` | `boolean` | - |
| `Date` | `string` | ISO 8601 格式 |
| `BigDecimal` | `string` | 金额等精确数值用字符串 |
| `List<T>` | `T[]` | - |

### 特殊字段处理

#### 状态字段

后端使用 `CHAR(1)` 存储状态，前端需要转换：

```java
// 后端 Entity
private String status;  // "0"=停用, "1"=正常
```

```typescript
// 前端类型
status: string  // "0" | "1"
```

#### 时间字段

后端返回的时间格式：

```java
// 后端 VO
@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
private Date createTime;
```

```typescript
// 前端类型
createTime: string  // "2026-02-09 10:30:00"
```

---

## 📋 VO/BO 对应关系

### 后端对象

| 对象类型 | 用途 | 前端对应 |
|---------|------|---------|
| **Bo** (Business Object) | 接收前端请求数据 | 表单提交数据 |
| **Vo** (View Object) | 返回给前端的数据 | 列表/详情展示 |
| **Query** (查询参数) | 分页查询条件（继承 PageQuery） | 查询表单数据 |

### 示例对应

**后端 XxxBo.java**：
```java
@Data
@AutoMapper(target = Xxx.class, reverseConvertGenerate = false)
public class XxxBo extends BaseEntity {
    @NotNull(message = "ID不能为空", groups = {EditGroup.class})
    private Long id;

    @NotBlank(message = "名称不能为空", groups = {AddGroup.class, EditGroup.class})
    private String xxxName;

    private String status;
}
```

**前端 xxxTypes.ts**：
```typescript
// 表单提交类型（对应 Bo）
export interface XxxForm {
  id?: number | string
  xxxName?: string
  status?: string
}

// 查询参数类型
export interface XxxQuery extends PageQuery {
  xxxName?: string
  status?: string
}

// 视图类型（对应 Vo）
export interface XxxVo {
  id: number | string
  xxxName: string
  status: string
  createTime: string
}
```

---

## 🔧 字典数据

### 后端字典表

字典数据存储在 `sys_dict_type` 和 `sys_dict_data` 表中。

### 常用字典类型

| 字典类型 | 说明 | 值 |
|---------|------|-----|
| `sys_normal_disable` | 状态 | 0=停用, 1=正常 |
| `sys_yes_no` | 是否 | Y=是, N=否 |
| `sys_user_sex` | 性别 | 0=男, 1=女, 2=未知 |

### 前端使用字典

前端通过 API 获取字典数据：

```
GET /system/dict/data/type/{dictType}
```

---

## 📁 菜单配置

### 后端菜单表

菜单配置存储在 `sys_menu` 表中，前端路由根据菜单动态生成。

### 菜单字段说明

| 字段 | 说明 | 前端用途 |
|------|------|---------|
| `menu_name` | 菜单名称 | 显示名称 |
| `path` | 路由地址 | 前端路由 path |
| `component` | 组件路径 | Vue 组件路径 |
| `perms` | 权限标识 | 权限控制 |
| `icon` | 菜单图标 | 侧边栏图标 |

### 组件路径规范

```sql
-- 后端菜单配置
component = 'demo/xxx/index'

-- 对应前端文件
plus-ui/src/views/demo/xxx/index.vue
```

---

## 🔄 开发流程

### 新功能开发流程

```
1. 后端开发
   ├── 设计数据库表
   ├── 创建 Entity、BO、VO
   ├── 创建 Mapper、Service、Controller
   └── 配置菜单和权限

2. 前端开发（plus-ui 项目）
   ├── 创建 API 接口和类型定义
   ├── 创建页面组件
   └── 配置路由（菜单自动生成）

3. 联调测试
   └── 前后端接口对接
```

### 后端提供给前端的信息

开发新功能时，后端需要提供：

1. **API 接口文档**
   - 请求路径
   - 请求方法
   - 请求参数（Bo 字段）
   - 响应数据（Vo 字段）

2. **权限标识**
   - 各操作对应的权限标识

3. **字典类型**
   - 状态等枚举字段对应的字典类型

---

## 📚 参考资料

### 后端参考

| 内容 | 文件位置 |
|------|---------|
| 后端开发指南 | `.claude/docs/后端开发指南.md` |
| CRUD 开发规范 | `.claude/docs/新功能开发流程规范.md` |
| 数据库设计规范 | `.claude/docs/数据库设计规范.md` |
| API 接口文档 | 启动后访问 `/doc.html` |

### 前端参考

前端开发请参考 plus-ui 项目的文档。

---

## ⚠️ 注意事项

### 本项目不包含前端代码

- ❌ 本项目（RuoYi-Vue-Plus）不包含任何前端代码
- ❌ 不要在本项目中创建 Vue/React 组件
- ❌ 不要在本项目中创建 TypeScript 类型定义
- ✅ 前端代码在独立的 plus-ui 项目中开发

### 后端开发注意事项

1. **API 路径使用标准 RESTful 格式**
   ```java
   @GetMapping("/list")      // ✅ 正确
   @GetMapping("/pageXxxs")  // ❌ 错误
   ```

2. **VO 中的 Long 类型字段注意精度**
   ```java
   // 超过 JS 安全整数范围的 ID 需要转为字符串
   @JsonSerialize(using = ToStringSerializer.class)
   private Long id;
   ```

3. **日期格式统一**
   ```java
   @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
   private Date createTime;
   ```

---

## 🎯 总结

| 项目 | 职责 | 本文档适用性 |
|------|------|-------------|
| **RuoYi-Vue-Plus**（本项目） | 后端 API 开发 | ✅ 后端开发规范 |
| **plus-ui** | 前端页面开发 | ❌ 请参考 plus-ui 项目文档 |

**核心原则**：

1. 本项目是**纯后端项目**
2. 前端项目**独立维护**
3. 前后端通过 **RESTful API** 交互
4. 权限标识**前后端保持一致**
5. 数据类型**注意 Java 与 TypeScript 的映射**
